// 1 - PENSAMENTO COMPUTACIONAL
    // DEFINIÇÃO: 
    // É uma decomposição(fragmentação) de uma ideia, problema. Seguindo até chegar ao algoritmo
    // Abstração: Isolar uma função especifica de um objeto e pensar sobre ela.
    // Capacidade de planejar, executar e gerenciar tarefas 
    // É um processo baseado em três estágios: abstração (formulação do problema), automação (expressão da solução), análise (execução e avaliação da solução).

    // EXEMPLOS DE SITUAÇÕES COTIDIANAS QUE VOCÊ APLICA O PENSAMENTO COMPUTACIONAL:
    // Trocar Pneu e cozinhar => Praticamente tudo 
    // Receita de bolo
    // Rotina antes de ir para o trabalho. Exemplo, conjuntos de tarefas estruturadas para ser executada dentro de um espaço de tempo.
    // Fazer sanduiche de pasta de amendoin com geleia(muito bom)
    // Treino na Academia
    // Fazer vitamina
    // Fritar ovo
    // Namorar uhhhhh garotos de programa O.O

// 2 - AMBIENTE DE DESENVOLVIMENTO
    // FALAR PRO COMPUTADOR
        // SABER qual o SO - Linux/Win/Mac > Identificar as configurações
        // LINGUAGEM - JavaScript *_* 
        // GERENCIADOR DE PACOTES/DEPEDÊNCIAS - npm
        // INTERPRETADOR - Node
        // FRAMEWORKS E BIBLIOTECAS [SEMPRE considerar a linguagem] - React, Angular, Vue, JQuery, Ajax
        // FERRAMENTA DE CONTROLE DE VERSÃO - git
        // NUVEM pro versionamento de código - github
        // IDE - vsCode

// 3 - FUNÇÕES
    // VARIÁVEIS - var, let, const
    // COMO SÃO USADAS VAR Global / LET local / CONST constante 
    // VAR: declara uma variável inicializando-a com um valor;
    // LET: declara uma variável de escopo do bloco, inicializando ela opcionalmente com um valor;
    // CONST: declara uma constante de escopo de bloco e somente leitura;
    
    // TIPOS DE DADOS - string, number, booleano -- array, objeto,  funcao 
    // o que acontece quando eu acesso uma variável que não existe ou deu erro?
    // NaN (not a number), NaN representa um numero fora do dominio do ECMA ( ligada a variavel number)
    // undefined, representa quando tentamos acessar uma variável fora do escopo, ou uma posição inexistente, ou alguma variável não inicializada ( ligada a variavel string)
    // null, representa um valor nulo ou "vazio" ( ligada a variavel booleana)
    // eles são tipos de dados? NÃO!

    // VERIFICAR O TIPO DE DADOS - método já definido do JS - 'type of'

    // ESTRUTURA DA FUNÇÃO
    // nome da função, palavra reservada 'function', return
    // parâmetro é opcional
    // É bloco de código que tem uma ação
    // PODE ser chamada em qualquer lugar do seu código
    // function DH (x,y){ return x+y }
    
// 4 - OPERADORES
    // DE ATRIBUIÇÃO --> '=' , '+=', '*=' (antes de fazer atribuição ele vai fazer a operação)
    // Referência: https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Guide/Expressions_and_Operators#operador_atribuicao

    // [fruta1, fruta2, fruta3, fruta4]
    // [a, b, c]
    // É uma desestruturação porque na sintaxe do código em questão,
    // quando a variável [fruta1, fruta2, fruta3] recebe um array com a mesma quantidade de posições,
    // o JS entende que ouve aconteceu uma atribuição via desestruturação,
    // que significa extrair dados de arrays ou objetos em variáveis distintas de acordo com a sua posição.
    // Sendo, a primeira posição da variável [fruta1, fruta2, fruta3] recebeu a primeira posição do array frutasAmarelas.
    // Referência: https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Reference/Operators/Destructuring_assignment

    // COMPARATIVOS --> '==' (valor) , '===' (tipo e valor - estritamente igual), '!=' (diferente), '!==' (estritamente não igual),
    // '>' (maior que), '<' (menor que), '<=' (menor ou igual), '>=' (maior ou igual) --> COMPARAÇÕES SIMPLES

    // '===' e '!==' --> COMPARAÇÃO ESTRITA sempre considerar valor e tipo

    // ARITMÉTICOS/MATEMÁTICOS --> + , -, *, /, ** (expoente), % (resto da divisão)
    // INCREMENTO/DECREMENTO --> ++ -- (i = i + 1 é o i++, ++i)
    
    // LÓGICOS --> '&&' (AND (E)), '||' (OR (OU)), '!x' (NOT (NEGAÇÃO))

    // DE STRING --> console.log("String X" + "String Y"); --> concatenação

    // VALIDAR TIPOS
        // ao invés de usar o ESTRITO, pode usar 'type of'

// 5 - CONTROLE DE FLUXO DA APLICAÇÃO
    // IF-ELSE
        // IF - ELSE IF - ELSE

    // CONDICIONAL(TERNÁRIO) 
        // --> (condição) ? resultado(true)1 : resultado 2 ;
        // (passa a condição, se for verdadeira retorna a primeira, os 2 pontos são o "else/ senão" passa o o falso)

    // SWITCH CASE

    

// 6 - STRING E ARRAY
 
// 7 - ESTRUTURA DE REPETIÇÃO
    // WHILE
    // DO-WHILE
   // FOR
                                             
// 8 - OBJETOS E JSON

// 9 - MÓDULOS E PACOTES

// 10 - FUNÇÕES AVANÇADAS